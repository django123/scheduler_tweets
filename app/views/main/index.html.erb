<div class="d-flex align-item-center  justify-content-center">
  <h1>Scheduler for tweets</h1>

  <% if @user %>
   Logged in as: <%= @user.email %>

    <%= button_to "logout", logout_path, method: :delete, class: 'btn btn-primary' %>
  <% end %>
</div>

